[project]
name = "lazy"
version = "1.1.0"
description = "Skript to provide colors for device roles based on json or csv color input"
readme = "datamodel_roles.md"
license = "MIT"
requires-python = ">=3.13,<3.14"

dependencies = [
    "PyYAML",
    "pandas",
    "loguru",
]
